

 importJPlots

   
         var  t = inc(0, 0.01, 20)
        var  x = sin(0.2*t)
        var  y = sin(0.2*t)+5*cos(0.23*t)
        var  z = exp(-0.2*y)
        jfigure(1)
        jhold(true)
        jplot(t, x,  "sin(0.2*t)")
        plot(t, y,  "sin(0.2*t)+5*cos(0.23*t)")
        plot(t, z,  "exp(-0.2*y)")
        xlabel("x-Time axis")
        ylabel("x-y-z plots")
        title("With hold on")


        